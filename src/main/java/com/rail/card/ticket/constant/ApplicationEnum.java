package com.rail.card.ticket.constant;

public class ApplicationEnum {
    public enum Group {
        ROLE_ADMIN,
        ROLE_USER
    }

    public enum Status {
        ACTIVE, INACTIVE, SUSPENDED
    }
}
